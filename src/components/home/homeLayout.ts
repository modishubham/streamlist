import {spacing} from '../../theme/spacing';

/** Visual height of `HomeHeader` row: paddingVertical + icon row. */
const HEADER_ICON_ROW = 28;

export const HOME_HEADER_CONTENT_HEIGHT =
  spacing.sm * 2 + HEADER_ICON_ROW;
